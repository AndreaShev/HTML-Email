код делаем атрибутами, а не стилями.
&nbsp; для нормального отображения кнопки в почте
комментарии (внутри которых табличная верстка) нужны для Outlook, т.к. вариант с display: inline-block не сработает 
тестируем в putsMail